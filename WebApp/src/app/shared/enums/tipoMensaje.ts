export enum TipoMensaje {
  Correcto = "success",
  Alerta = "warning",
  Error = "danger",
}

export enum MensajesSustitución {
  NoExistenResultados = "No existen resultados a sustituir para los años seleccionados",
  PrimeraVez = "Es la primera vez que se realizará la sustitución, da clic en 'Sustituir limites'"
}
